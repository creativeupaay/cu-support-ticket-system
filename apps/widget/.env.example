# Support Hub Widget Environment Configuration

# Production API Base URL (leave empty in local development to use relative /api proxy)
# In Cloudflare Pages, set this to your deployed GCP backend URL (e.g. https://api-xyz.a.run.app)
VITE_API_BASE_URL=
